import { NumeroPunteadoPipe } from './numero-punteado.pipe';

describe('NumeroPunteadoPipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroPunteadoPipe();
    expect(pipe).toBeTruthy();
  });
});
